#ifndef HELPER_H
#define HELPER_H 

#define ARRAY_SIZE(array)		(sizeof(array)/sizeof(*array))

#define MAX(a, b)			(a > b ? a : b)
#define MIN(a, b)			(a < b ? a : b)

#define ever				(;;)

#endif /* HELPER_H */
